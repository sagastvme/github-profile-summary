# wsgi.py
from app import app

# If you need any extra WSGI middlewares, add them here, e.g.:
# app.wsgi_app = ProxyFix(app.wsgi_app)
# But for a simple case, just import the Flask 'app' and that's it.
